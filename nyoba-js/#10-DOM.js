// Dom document object model
// is kind of document for code, there's rule for it


// example in js
const paragraphs = document.querySelectorAll('p');
// paragraphs[0] is the first <p> element

alert(paragraphs[0].nodeName);

// when web page downloading, browser automatic make a DOM page
// the object doc access with window.document ar just document


